# Developer-only dependencies (not needed at runtime)
# Prefer installing via `pip install -e .[dev]`, this file remains for convenience.
pytest>=8.0
pytest-cov>=5.0
mypy>=1.10
ruff>=0.5.0
pyinstaller>=6.11
